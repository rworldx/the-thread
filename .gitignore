node_modules/
.next/
out/
dist/

# Secrets. This line exists before any key does.
.env
.env.local
.env*.local

.DS_Store
*.log
coverage/
next-env.d.ts
test-results/
playwright-report/
screenshots/
*.tsbuildinfo

# Design skill references — local only, never committed
Skills/
Thmanyah-Font-Family/

# Working documents and agent instructions. Local only: the repository ships
# one document, and it is the README. AGENTS.md is regenerated by `next dev`
# on every run, so it is ignored rather than deleted.
AGENTS.md
CLAUDE.md
docs/
